package strategy.spotAssignmentStrategy;

import models.Gate;
import models.ParkingSpot;
import models.VehicleType;

public class CheapestSpotAssignmentStrategy implements SpotAssignmentStrategy {

    @Override
    public ParkingSpot spotAssignment(VehicleType vehicleType, Gate gate) {
        return null;
    }
}
